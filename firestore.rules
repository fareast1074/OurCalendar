rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple app mode: no Firebase Authentication is used.
    // Anyone who can access this Firestore project can read/write the data.
    // This matches the requested lightweight username + password app.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
